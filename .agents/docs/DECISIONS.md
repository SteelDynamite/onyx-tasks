---
doc-manifest:
  validation:
    frontmatter:
      keywords:
        type: string[]
    required-sections: [Context, Options considered, Decision, Consequences]
    filename-pattern: "[0-9][0-9][0-9][0-9]-*.md"
---
Records of consequential choices embedded in the current project state that are hard to reverse, surprising without context, or based on real tradeoffs. Update, replace, or delete them in place as the current design changes; version control preserves superseded history.
